class
   Normandy.Json.Declarations

inherit
   Aqua.Text_IO
   Normandy.Json.Lists

feature

    Before_Declaration (Child : Normandy.Json.Declaration)
        do
            Child.Set_Lines (Lines)
        end

end
