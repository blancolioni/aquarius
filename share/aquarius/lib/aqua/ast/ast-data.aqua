deferred class
   Ast.Data

inherit
   Ast

--  A data-section item. Data ops need no active routine, so these are
--  generated by Ast.Unit after its routines. Generate (inherited, deferred)
--  emits the item.

end
